# Google Cloud project used for BigQuery-grounded estimation
GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# Gemini model used by the multi-agent system
GEMINI_MODEL=gemini-2.5-flash

# Optional: service account credentials for local or containerized execution
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
